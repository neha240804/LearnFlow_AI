// XP rewards per learning activity
export const XP_PER_STAGE_COMPLETE = 10;
export const XP_PER_CORRECT_QUIZ = 20;
export const XP_BONUS_EXCELLENCE = 15;

// Adaptive learning thresholds (0–1 normalised quiz score)
export const ADAPTIVE_LOW_THRESHOLD = 0.4;   // below this → extra support needed
export const ADAPTIVE_HIGH_THRESHOLD = 0.85; // at or above this → excellence bonus
